---
componentSets:
       - name: bootstrap
         blocks:
            - name: pagination
      #  - name: <set_name, i.e. bootstrap>
      #    blocks:
      #       - name: <component_block_name, i.e. pagination>
partialSets:
      #  - name: <set_name, i.e. default>
      #    blocks:
      #       - name: <partial_block_name, i.e. landing>
          